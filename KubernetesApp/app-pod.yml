apiVersion: v1
kind: Pod
metadata:
  name: app-pod
  labels:
    app: app
    type: mvc
spec:
  containers:
    - name: app-pod
      image: josephcarazo/apilab01:v1.0
      ports:
        - containerPort: 3000
